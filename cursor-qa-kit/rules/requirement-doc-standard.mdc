---
description: 生成/整理测试侧需求文档（test_*-requirements.md）时必须提取 PRD 内嵌测试用例与验收标准
globs: "**/test_*-requirements.md,**/PRD*.md,**/PRD-*.md,**/产品需求/**/*.md"
alwaysApply: false
---

# 测试需求文档生成规范

适用：整理 `test_[功能名]-requirements.md`、阅读产品 PRD 产出测试可读需求清单。

## 必读来源（按优先级）

1. **子 PRD**（冲突时为准）
2. **父 PRD** 通用规则与异常表
3. **PRD 内「验收标准 / 测试用例」章节**（场景 + 期望值，与功能描述同等重要）
4. 后端接口文档、前端开发规范

## PRD 测试用例处理（强制）

- 全文检索：`验收标准`、`测试用例`、`测试场景`、`步骤`、`预期结果`、`期望结果`
- **操作与期望结果原文保留**，写入需求文档 **§9 PRD 验收与测试场景**
- 能映射到字段/流程的，在 §3～§6 同步体现，并标注 `〔PRD §x.x〕`
- PRD 测试用例已明确的期望 **不得** 标为 `[待澄清]`
- PRD 与其他文档冲突：正文以子 PRD 为准，标注 `⚠️ 与 [文档名] 冲突`

## 有长度限制的字段

须补充 **最短/最长合法值** 在表单、预览（若有）、列表、详情等处的 **完整展示** 要求；PRD 测试用例已描述则照录。

## 文档结构

必须包含章节：**§9 PRD 验收与测试场景**（PRD 无测试章节时说明并从功能描述推导场景列表）。

## 输出

- 路径：`test/requirements/test_[功能名]-requirements.md`（见 `test-engineer-conventions.mdc`）
- 整理完成后提示：§9 可作为测试用例步骤/预期的溯源
