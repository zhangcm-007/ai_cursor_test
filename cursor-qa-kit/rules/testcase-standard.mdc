---
description: 测试用例生成标准规范
alwaysApply: true
---

# 测试用例生成规范

## 1. 用例字段（固定，不可省略）

| 字段 | 说明 |
|------|------|
| 编号 | 全局自增，格式 TC-001、TC-002，整个文档统一连续编号 |
| 标题 | 动宾结构，简明描述操作意图，不超过 20 字 |
| 测试点 | 本条用例验证的核心点，如：必填项校验、边界值、权限隔离、幂等性、状态流转等 |
| 用例类型 | 功能测试 / 接口测试 / UI 测试 / 性能测试 / 兼容性测试 |
| 前置条件 | 执行用例前必须满足的环境/数据状态 |
| 步骤 | 有序操作步骤，格式：1.xxx → 2.xxx |
| 预期结果 | 与步骤一一对应的期望结果，格式：1.xxx → 2.xxx |
| 实际结果 | 执行后填写，未执行时留空 |
| 执行状态 | Pass / Fail / Block / Skip |
| 优先级 | P0 / P1 / P2 / P3（见优先级定义） |

## 2. 覆盖维度（每个需求至少覆盖以下场景）

- **正向**：标准输入，验证主流程成功路径
- **反向**：无效/错误输入，验证系统拒绝并给出正确提示
- **必填项校验**：逐一置空每个必填字段，验证系统给出明确提示且不提交
- **边界**：临界值（最大值、最小值、空值、临界长度）
- **异常**：网络中断、服务超时、数据库异常等不可控情况
- **并发幂等**：重复提交、并发操作是否产生脏数据或重复记录
- **权限**：不同角色/未登录/越权访问的隔离验证
- **性能**：接口响应时间、页面加载时长是否在合理阈值内（仅在生成接口测试用例时关注，纯功能用例可忽略）
- **兼容性**：浏览器（Chrome/Firefox/Safari/Edge）、操作系统、移动端分辨率
- **数据隔离**：多租户/多用户场景下，用户间数据不互相污染
- **回滚/撤销**：操作失败后系统状态能正确还原，事务回滚生效

## 3. 必填项校验规则

凡是含有表单/输入项的功能，必须覆盖以下必填项场景：

| 场景 | 说明 |
|------|------|
| 单字段为空 | 逐一置空每个必填字段，其他字段正常填写，验证提示信息准确指向该字段 |
| 全部字段为空 | 所有字段留空直接提交，验证提示第一个必填字段错误 |
| 仅空格 | 字段只输入空格，验证系统识别为空并给出提示 |
| 提示文案 | 提示内容需明确，如"XXX不能为空"，禁止模糊提示如"请填写完整信息" |
| 提交拦截 | 必填项未填时不可发送网络请求，在前端即拦截 |

## 4. 标题规范

- **必须**使用动宾结构，例如：`提交订单`、`校验手机号格式`、`拒绝越权访问`
- **禁止**使用 `测试 xxx 功能`、`验证 xxx` 等模糊空话
- 标题应直接体现操作行为和场景，不超过 20 字

## 5. 优先级定义

| 优先级 | 含义 | 执行要求 |
|--------|------|----------|
| P0 | 冒烟用例，阻塞上线 | 必须 100% 通过，任何 Fail 立即停止提测 |
| P1 | 核心业务流程 | 每个版本必测，Fail 数量不超过 0 |
| P2 | 一般功能场景 | 按需执行，Fail 可挂起评估影响范围 |
| P3 | 边缘/低频场景 | 时间充裕时执行，可延期到下个迭代 |

## 6. 用例组织结构

按三层结构组织，严格不可打乱层级：

```
# 模块名                        ← 一级标题（如：登录模块）
## 子模块名                     ← 二级标题，直接写名称，不加"子模块："前缀
表格（每个子模块一张独立表格）   ← 具体用例行，编号全局连续
```

- 编号格式：`TC-001`，整个文档全局自增，不重置
- 跨子模块、跨模块编号连续，不从 001 重新开始
- **落盘路径**：`test/testcases/test_[功能模块名]-testcases.md`（目录与 `test_` 前缀见 `test-engineer-conventions.mdc`）
- **mm 文件顶节点**：使用功能模块名称，如 `手动创建Skill`

## 6.1 具体测试数据要求

测试步骤和预期结果中的输入内容必须使用**具体的测试数据值**，禁止用描述性语言替代：

| 禁止写法 | 正确写法 |
|----------|----------|
| 描述输入50字合法内容 | 描述输入「当用户询问A股行情时，本Skill提供实时股价、涨跌幅及成交量数据，辅助用户做出更明智的投资决策，适合个人投资者日常使用」 |
| 输入超长字符串 | 密码框输入「abcdefghijklmnopqrstuvwxyz0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789abcdefghijklmnopqrstuvwxyz0123456789abcdefghijklmnopqrstuvwxyz0123456789abcdefghijklmnopqrstuvwxyz0123456789abcdefghijklmnopqrstuvwxyz0123456789ABCDE」（共210字符） |
| 输入合法值 | 输入 `buffett-audit` |
| 输入非法格式 | 输入 `MySkill@name` |

## 7. 输出格式

- 主要输出：**Markdown 表格**（方便在对话中直接阅读）
- 同时提供：**xlsx 格式** —— 列序、合并单元格、样式、命名等 **完整规范见** `testcase-excel-export-standard.mdc`（与 `export_excel.py` 一致）
- 同时提供：**mm 格式**（FreeMind XML，可导入 XMind；业务用例由导出脚本生成，规范结构参考同目录 `testcase-standard.mm`）
- 步骤与预期结果字段内多步骤使用 ` → ` 分隔，如：`1.输入用户名 → 2.点击登录`

## 8. 示例

# 登录模块

## 子模块：密码登录

| 编号 | 标题 | 测试点 | 用例类型 | 前置条件 | 步骤 | 预期结果 | 实际结果 | 执行状态 | 优先级 |
|------|------|--------|----------|----------|------|----------|----------|----------|--------|
| TC-001 | 提交有效密码登录 | 正向主流程 | 功能测试 | 用户已注册，服务正常 | 1.输入正确邮箱和密码 → 2.点击登录 | 1.- → 2.跳转首页，显示用户昵称 | | | P0 |
| TC-002 | 拒绝空密码提交 | 必填项校验 | 功能测试 | 登录页正常加载 | 1.输入邮箱，密码留空 → 2.点击登录 | 1.- → 2.前端拦截，提示"密码不能为空"，不发送请求 | | | P1 |
| TC-003 | 拒绝邮箱和密码全部为空提交 | 必填项校验 | 功能测试 | 登录页正常加载 | 1.邮箱和密码均留空 → 2.点击登录 | 1.- → 2.提示"邮箱不能为空"，不发送请求 | | | P1 |
| TC-004 | 限制超长密码输入 | 字段长度边界 | 功能测试 | 登录页正常加载 | 1.密码框粘贴 256 个字符 → 2.点击登录 | 1.输入框截断或提示超出限制 → 2.不发送请求 | | | P2 |

## 子模块：验证码登录

| 编号 | 标题 | 测试点 | 用例类型 | 前置条件 | 步骤 | 预期结果 | 实际结果 | 执行状态 | 优先级 |
|------|------|--------|----------|----------|------|----------|----------|----------|--------|
| TC-005 | 发送验证码并成功登录 | 正向主流程 | 功能测试 | 邮箱已注册，网络正常 | 1.输入邮箱 → 2.点击发送验证码 → 3.输入正确验证码 → 4.点击登录 | 1.- → 2.按钮进入60s倒计时 → 3.- → 4.跳转首页 | | | P0 |
| TC-006 | 拒绝过期验证码登录 | 验证码时效 | 功能测试 | 验证码已超过有效期 | 1.输入邮箱和过期验证码 → 2.点击登录 | 1.- → 2.提示"验证码已过期，请重新获取" | | | P1 |
