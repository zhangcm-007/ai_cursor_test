---
description: 测试用例 xlsx/mm 导出格式规范（与 export_excel.py 一致）；生成或导出用例表格时须遵守
alwaysApply: true
---

# 测试用例 Excel（xlsx）导出规范

> 实现脚本：`%USERPROFILE%\.cursor\skills\testcase-generator\scripts\export_excel.py`  
> 源文件：符合 `testcase-standard.mdc` 的 Markdown 用例（`test_*-testcases.md`）。

## 1. 总则

- 每个功能模块对应 **1 个 xlsx 文件**，与 md 同名（如 `test_手动创建Skill-testcases.xlsx`）。
- 工作簿内 **仅 1 个 Sheet**，汇总该 md 中全部用例，**禁止**按模块拆多 Sheet。
- Sheet 名称：取 md 文件名（去扩展名），非法字符 `\ / * ? : [ ]` 替换为 `-`，最长 **31** 字符。
- 生成流程：先写 md → 再执行导出脚本；**禁止**手写 xlsx 偏离下列列序与合并规则。

```bash
python "%USERPROFILE%\.cursor\skills\testcase-generator\scripts\export_excel.py" <markdown_file> [output_dir]
```

依赖：`pip install openpyxl`

---

## 2. Markdown 源文件映射

| Markdown 结构 | Excel 列 / 行为 |
|---------------|-----------------|
| `# 一级标题` | **模块**列取值；**标题**列导出时加 `【一级标题】` 前缀 |
| `## 二级标题` | **子模块**列取值 |
| 表格数据行（10 列） | 见下表「md 原始列」→ 展开为 Excel 12 列 |

**md 表格原始列（10 列，顺序固定）**：

编号、标题、测试点、用例类型、前置条件、步骤、预期结果、实际结果、执行状态、优先级

**步骤 / 预期**：单元格内多步使用 ` → ` 连接，如 `1.输入邮箱 → 2.点击登录`。

---

## 3. Excel 表头与列序（12 列，不可调换）

| 列 | 列名 | 宽度 | 说明 |
|----|------|------|------|
| A | 编号 | 10 | 与 md 一致，如 TC-001 |
| B | 模块 | 14 | 来自 `# 一级标题`；连续相同值 **纵向合并单元格** |
| C | 子模块 | 16 | 来自 `## 二级标题`；同一 (模块,子模块) 从第 2 行起置空（视觉合并） |
| D | 标题 | 28 | 导出值 = `【模块名】` + md 原标题（md 内不写前缀） |
| E | 测试点 | 16 | |
| F | 用例类型 | 14 | |
| G | 前置条件 | 20 | |
| H | 步骤 | 40 | 自动换行 |
| I | 预期结果 | 40 | 自动换行 |
| J | 实际结果 | 14 | 执行后填写，生成时留空 |
| K | 执行状态 | 12 | Pass / Fail / Block / Skip |
| L | 优先级 | 10 | P0 / P1 / P2 / P3 |

---

## 4. 样式与版式

| 区域 | 规则 |
|------|------|
| 表头行（第 1 行） | 背景 `#4472C4`，白字加粗，水平/垂直居中，细边框 |
| **模块**列（B） | 背景 `#E2EFDA`，加粗 |
| **子模块**列（C） | 每个 (模块,子模块) 首行背景 `#D9E1F2` 加粗；同组后续行 C 列留空 |
| 数据区 | 全部单元格：自动换行、顶端对齐、细边框 `#CCCCCC` |
| 冻结窗格 | `A2`（表头固定） |

---

## 5. 合并与标题前缀规则

1. **模块列合并**：B 列中连续相同非空模块名，纵向 `merge_cells`（值保留左上角）。
2. **标题前缀**：仅出现在 **Excel D 列**，格式 `【{一级标题}】{原标题}`；Markdown 表格标题列保持纯标题。
3. **子模块列**：不执行 Excel merge，通过同组后续行置空实现分组阅读。

---

## 6. 输出文件命名

| 类型 | 命名 |
|------|------|
| xlsx | 与 md 同 basename，扩展名 `.xlsx` |
| mm | 与 md 同 basename，扩展名 `.mm`（FreeMind XML，由同一脚本生成） |

示例（均在 `test/testcases/` 下）：

```
test/testcases/test_手动创建Skill-testcases.md
test/testcases/test_手动创建Skill-testcases.xlsx   ← 单 Sheet
test/testcases/test_手动创建Skill-testcases.mm
```

目录约定见 `test-engineer-conventions.mdc`。

---

## 7. mm 导出（同脚本）

- 顶节点：md 中**第一个** `# 一级标题`（无则「测试用例」）。
- 二级：按**子模块**分组；叶子节点：`[编号] 标题｜优先级`。
- 子节点字段：测试点、用例类型、前置条件、步骤、预期结果（有值才输出）。

---

## 8. 校验清单（导出前）

- [ ] md 为三层结构：`# 模块` → `## 子模块` → 10 列表格
- [ ] 编号 TC 全局连续
- [ ] 已运行 `export_excel.py`，未手改列序
- [ ] xlsx 仅 1 个 Sheet，B 列主模块已合并
- [ ] D 列标题含 `【模块】` 前缀，md 中无前缀
