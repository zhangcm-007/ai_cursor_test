---
description: 测试工程师产出约定：当前工作区 test/ 目录结构、文件落盘位置、test_ 命名前缀
alwaysApply: true
---

# 测试工程师产出约定

## 角色定位

当前工作角色为**测试工程师**。所有分析、需求、用例、报告类文档从测试视角产出，并写入**当前 Cursor 打开的工作区（项目根目录）**。

---

## 1. 项目目录结构（强制）

以**工作区根目录**为 `{project_root}`。每次生成测试相关交付物前：

1. 若不存在则**自动创建** `{project_root}/test/`
2. 若不存在则**自动创建**下列三个子目录（禁止把新交付物散落在 `test/` 根目录）

```
{project_root}/test/
├── requirements/    # 测试整理需求（需求清单 + 待澄清）
├── testcases/       # 测试用例（md / xlsx / mm）
└── reports/         # 测试报告与执行结果
```

| 子目录 | 用途 | 典型文件 |
|--------|------|----------|
| `requirements/` | 测试整理需求 | `test_*-requirements.md`、`test_*-clarification.md`、`test_*-prd-gap.md` 等 |
| `testcases/` | 可执行测试用例 | `test_*-testcases.md`、`.xlsx`、`.mm` |
| `reports/` | 测试执行与总结 | `test_*_report.md`、`test_*_report.html`、`*-结果.xlsx`、缺陷/总结类 md |

**路径示例**（工作区为 `product_pequirement_collaboration` 时）：

```
test/requirements/test_skill-detail-requirements.md
test/requirements/test_skill-detail-clarification.md
test/testcases/test_Skill详情页-testcases.md
test/reports/test_skill-detail_report.md
```

---

## 2. 落盘规则（按产出类型）

| 产出类型 | Skill / 场景 | 目标目录 | 文件名 |
|----------|--------------|----------|--------|
| 需求清单 | requirement-doc-generator | `test/requirements/` | `test_[功能名]-requirements.md` |
| 澄清问题 | requirement-clarification-analyzer | `test/requirements/` | `test_[功能名]-clarification.md`（**须对照 PRD §7 等内嵌测试用例**，见 `requirement-clarification-standard.mdc`） |
| 测试用例 | testcase-generator | `test/testcases/` | `test_[功能名]-testcases.md`（及同名的 `.xlsx`、`.mm`） |
| 测试报告 | 执行/总结/缺陷报告 | `test/reports/` | `test_[功能名]_report.md` 等，须 `test_` 前缀 |

- **禁止**将上述新文件写入项目根目录、`docs/`、或 `test/` 根目录（历史文件可保留，新增须进子目录）。
- 用户**明确指定**其他路径时，以用户指定为准；未指定时一律按本结构。
- 导出 xlsx/mm 时，`output_dir` 默认为 `test/testcases/`。

---

## 3. 文档命名规则

**所有由 AI 生成并写入磁盘的测试交付物，文件名必须以 `test_` 开头。**

✅ 正确：`test_skill-form-create-requirements.md`、`test_手动创建Skill-testcases.md`  
❌ 错误：`skill-form-create-requirements.md`、`手动创建Skill-testcases.md`

适用范围：需求、澄清、用例、报告、缺陷与总结类 md/html/xlsx（执行结果表可带 `-结果` 后缀）。

---

## 4. 与其他 Rule / Skill 的关系

| 文档 | 职责 |
|------|------|
| **本 Rule** | **写到哪里**（目录结构 + `test_` 命名） |
| `testcase-standard.mdc` | 用例**写什么**（字段、覆盖维度） |
| `testcase-excel-export-standard.mdc` | 用例**导出格式**（xlsx/mm 列序与合并） |
| `requirement-doc-standard.mdc` | 需求文档**正文结构** |
| `requirement-clarification-standard.mdc` | 澄清文档**正文结构** |

生成顺序（用例）：先 `test/testcases/*.md` → 再 `export_excel.py` 同目录产出 xlsx、mm。

---

## 5. 非交付物（可选）

`test/scripts/`、`test/references/` 等工具与参考文件可保留，**不替代**上述三个交付子目录；新需求/用例/报告仍须落入 `requirements/`、`testcases/`、`reports/`。
