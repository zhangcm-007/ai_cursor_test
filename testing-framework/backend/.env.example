# ========== 大模型三选一（按优先级：Dify > 公司大模型 > Claude） ==========

# ---------- 方式一：Dify 平台（推荐，用公司 Dify） ----------
# Dify 应用 API 地址（在 Dify 应用「API 访问」里复制）；也可用 DIFY_BASE_URL 或 DIFY_BASE
# DIFY_API_BASE=https://your-dify.company.com/v1
# Dify 应用 API Key（在 Dify 应用「API 访问」里复制）；也可用 DIFY_KEY
# DIFY_API_KEY=app-xxxx
# 可选：图片识别专用 Agent（新建 Dify Agent，选 Vllm 模型）
# 不配置则图片附件不解析。Base 默认与上面相同，仅 Key 换为新 Agent 的 Key。
# 重要：在 Dify 中必须让 Agent 能“看到”API 传入的图片：
#   1. 应用设置里开启「支持文件上传」/ 允许图片；
#   2. 使用的模型需支持多模态（Vision）；若为工作流，在 LLM 节点中启用 Vision 并选择传入的文件（如 sys.files 或对应输入变量）。
# DIFY_IMAGE_AGENT_API_KEY=app-yyyy
# DIFY_IMAGE_AGENT_BASE=https://your-dify.company.com/v1
# 可选：公司内部图片解析接口（OpenAI 兼容 /v1/chat/completions，支持 image_url）
# 配置后图片附件优先走该接口，未配置或请求失败时再尝试 Dify 图片 Agent
# COMPANY_IMAGE_API_BASE=http://8.215.74.54:9600
# COMPANY_IMAGE_API_KEY=   # 若接口需鉴权则填写

# ---------- 方式二：公司大模型（OpenAI 兼容接口） ----------
# 公司大模型 API 基础地址，需兼容 OpenAI /chat/completions
# LLM_BASE_URL=https://your-llm.company.com/v1
# LLM_API_KEY=your-company-llm-key
# 可选：模型名
# LLM_MODEL=gpt-3.5-turbo

# ---------- 方式三：Claude（需自行充值） ----------
# ANTHROPIC_API_KEY=sk-ant-xxx
# LLM_MODEL=claude-opus-4-6
